import { ReservationList } from '@/components/pages';
import { Layout } from '@/components/templates';
export default function Home() {
  return (
    <Layout>
      <ReservationList />
    </Layout>
  );
}
